name: Test

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run action
        uses: TechWiz-3/python-gh-action@master
        #with:
        #  files: test.md
        #  blacklist:  https://www.github.com/paramt/this-doesnt-exist
